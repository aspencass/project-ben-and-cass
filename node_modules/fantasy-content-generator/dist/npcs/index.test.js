"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var index_1 = __importDefault(require("./index"));
var Utils = __importStar(require("../utils"));
describe("NPCs", function () {
    test("generate()", function () {
        Utils.forCount(50, function () {
            var npc = index_1.default.generate();
            expect(typeof npc).toEqual("object");
            Object.keys(npc).forEach(function (key) {
                expect(npc[key]).not.toEqual("undefined");
            });
        });
    });
    it("test with seed", function () {
        var baseNpc = index_1.default.generate({ seed: "123" });
        Utils.forCount(50, function () {
            var npc = index_1.default.generate({ seed: "123" });
            expect(baseNpc.formattedData.name).toEqual(npc.formattedData.name);
            expect(baseNpc.formattedData.traits[0]).toEqual(npc.formattedData.traits[0]);
        });
    });
});
